{
  "name": "@open-home/hub",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "open-home hub — API, event bus, and the agent that runs the home.",
  "license": "AGPL-3.0-or-later",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
